{
  "name": "maka",
  "version": "0.1.0",
  "private": true,
  "packageManager": "npm@11.12.1",
  "type": "module",
  "workspaces": [
    "packages/core",
    "packages/storage",
    "packages/runtime",
    "packages/headless",
    "packages/ui",
    "apps/desktop"
  ],
  "scripts": {
    "typecheck": "npm run typecheck --workspaces --if-present",
    "test": "npm run test:scripts && npm --workspace @maka/core test && npm --workspace @maka/storage test && npm --workspace @maka/runtime test && npm --workspace @maka/headless test && npm --workspace @maka/ui test && npm --workspace @maka/desktop test",
    "test:scripts": "node --test scripts/run-headless-tests.test.mjs",
    "dev": "npm --workspace @maka/desktop run dev --",
    "dev:full": "npm run build && npm --workspace @maka/desktop run start",
    "build": "npm --workspace @maka/core run build && npm --workspace @maka/storage run build && npm --workspace @maka/runtime run build && npm --workspace @maka/headless run build && npm --workspace @maka/ui run build && npm --workspace @maka/desktop run build",
    "clean": "node scripts/clean-build.mjs",
    "rebuild": "npm run clean && npm run build",
    "check:stale": "node scripts/check-stale-dist.mjs",
    "prepare:officecli": "node scripts/prepare-officecli.mjs",
    "check:officecli-bundle": "node scripts/check-officecli-bundle.mjs",
    "check:release": "npm run check:stale && npm run check:officecli-bundle && node scripts/check-dead-css.mjs --check",
    "check:chat-visual": "electron scripts/check-chat-marker-computed-style.mjs",
    "cost:deepseek-baseline": "node scripts/deepseek-live-cost-baseline.mjs"
  },
  "devDependencies": {
    "@types/node": "^25.0.0",
    "typescript": "^5.9.3"
  },
  "allowScripts": {
    "electron@39.8.10": true,
    "esbuild@0.27.7": true,
    "@jackwener/opencli@1.8.4": true
  }
}
